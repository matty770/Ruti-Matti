import { DirBorderDirective } from './dir-border.directive';

describe('DirBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new DirBorderDirective();
    expect(directive).toBeTruthy();
  });
});
